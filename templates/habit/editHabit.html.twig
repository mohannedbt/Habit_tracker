{% extends 'base.html.twig' %}

{% block body %}
    <div class="container mt-4">
        <h1>Edit Habit</h1>
        {{ form_start(form) }}
        {{ form_widget(form) }}
        <button class="btn btn-success mt-3">Save Changes</button>
        {{ form_end(form) }}
    </div>
{% endblock %}
